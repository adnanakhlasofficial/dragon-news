# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
env:
    VITE_apiKey: ${{ secrets. AIzaSyCF68IGEOb8JmD-2S5ZOjaYooyb3A7xZ44}}
    VITE_authDomain: ${{ secrets. dragon-news-da.firebaseapp.com}}
    VITE_projectId: ${{ secrets. dragon-news-da}}
    VITE_storageBucket: ${{ secrets. dragon-news-da.firebasestorage.app}}
    VITE_messagingSenderId: ${{ secrets. 866699625410}}
    VITE_appId: ${{ secrets. 1:866699625410:web:ec40e8665bbf6adcc98c5b}}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DRAGON_NEWS_DA }}
          channelId: live
          projectId: dragon-news-da
